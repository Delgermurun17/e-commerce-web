// import express from "express"
// import connectDB from "../config/db"

// const app= express()
// const PORT=4000
// app.use(express)
// connectDB()

// app.get("/", (req: any, res: {send: (arg: string)=>void}))